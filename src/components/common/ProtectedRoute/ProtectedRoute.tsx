import { ProtectedRoute as _ProtectedRoute } from '@nimoh-digital-solutions/tast-ui';

// Default export preserved so existing imports of `import ProtectedRoute from './ProtectedRoute'`
// continue to work while the implementation comes from @nimoh-digital-solutions/tast-ui.
export default _ProtectedRoute;
