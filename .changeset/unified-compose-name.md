---
"@nimoh-digital-solutions/create-tast-app": patch
---

Use same docker-compose project name as BE so all containers are grouped together in Docker Desktop — container names (with -be-/-fe- prefixes) are the differentiators
