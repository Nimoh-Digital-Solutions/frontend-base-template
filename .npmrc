# All @nimoh-digital-solutions scoped packages are published to the public npm
# registry (https://registry.npmjs.org). No special configuration is needed.
#
# NOTE: Yarn 4 does NOT read .npmrc — config lives in .yarnrc.yml.
# This file is kept for npm / Yarn 1 / pnpm compatibility.
